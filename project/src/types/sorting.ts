export type SortingOption = {
  label: string;
  value: string;
}

export type SortingOptions = {
  DEFAULT: SortingOption;
  PRICE_HIGH: SortingOption;
  PRICE_LOW: SortingOption;
  RATING: SortingOption;
}
